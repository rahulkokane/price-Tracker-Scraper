# DB: For SQLite the DATABASE_URL is a filepath. For Postgres use postgresql://user:pass@host:port/dbname
DATABASE_URL=

# Simple config
CACHE_TTL_HOURS=12
CONCURRENCY=5
GLOBAL_DELAY_SECONDS=14.4
RETRIES=3
TIMEOUT=12

# Optional: add proxies comma-separated or leave empty
PROXIES=
